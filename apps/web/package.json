{
  "name": "@sumi-os/docs-web",
  "version": "0.1.0",
  "description": "Bilingual Astro and Starlight handbook with a machine-readable MCP projection.",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.26.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/starSumi/sumi-docs.git",
    "directory": "apps/web"
  },
  "homepage": "https://github.com/starSumi/sumi-docs/tree/main/apps/web#readme",
  "bugs": {
    "url": "https://github.com/starSumi/sumi-docs/issues"
  },
  "author": "Sumi OS",
  "license": "MIT",
  "engines": {
    "node": ">=25.5.0"
  },
  "volta": {
    "extends": "../../package.json"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build && node --run verify:dist",
    "build:machine-projection": "node scripts/build-machine-projection.mjs",
    "build:release": "node --run validate:release-env && node --run build",
    "preview": "astro preview",
    "check": "astro check",
    "test": "node --test tests/**/*.test.mjs",
    "format:check": "prettier --check . --ignore-path .gitignore",
    "lint:staged": "lint-staged --fail-on-changes",
    "commitlint": "commitlint",
    "test:git-hooks": "node scripts/git-hooks/test-gates.mjs",
    "verify:dist": "node scripts/verify-dist.mjs",
    "verify:mcp": "node scripts/verify-mcp.mjs",
    "validate:release-env": "node scripts/validate-release-env.mjs",
    "audit:prod": "node ../../scripts/run-pnpm-command.mjs audit --prod --audit-level high --registry https://registry.npmjs.org",
    "verify": "node --run format:check && node --run test && node --run test:git-hooks && node --run build",
    "verify:release": "node --run format:check && node --run test && node --run build:release && node --run audit:prod",
    "verify:push": "node --run verify && node --run audit:prod"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.41.7",
    "@sumi-os/corpus-contract": "workspace:*",
    "astro": "^7.2.3",
    "sharp": "^0.35.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^21.2.0",
    "husky": "^9.1.7",
    "lint-staged": "^17.3.0",
    "prettier": "^3.9.6",
    "starlight-typedoc": "0.23.1",
    "typedoc": "0.28.20",
    "typedoc-plugin-markdown": "4.12.0",
    "typescript": "^5.9.3"
  },
  "lint-staged": {
    "*.{js,mjs,cjs,ts,astro,json,md,mdx,css,yaml,yml}": "prettier --check"
  }
}
