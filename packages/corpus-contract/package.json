{
  "name": "@sumi-os/corpus-contract",
  "version": "0.1.0",
  "description": "Versioned Sumi documentation corpus schemas and conformance helpers.",
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/starSumi/sumi-docs.git",
    "directory": "packages/corpus-contract"
  },
  "homepage": "https://github.com/starSumi/sumi-docs/tree/main/packages/corpus-contract#readme",
  "bugs": {
    "url": "https://github.com/starSumi/sumi-docs/issues"
  },
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./schemas/*.json": "./schemas/*.json",
    "./fixtures/*": "./fixtures/*"
  },
  "files": [
    "dist",
    "schemas",
    "fixtures",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "node --run clean && tsc",
    "clean": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\"",
    "typecheck": "tsc --noEmit",
    "test": "node --run build && node --test tests/**/*.test.mjs",
    "verify": "node --run typecheck && node --run test",
    "prepack": "node --run build"
  },
  "engines": {
    "node": ">=25.5.0"
  },
  "volta": {
    "extends": "../../package.json"
  },
  "devDependencies": {
    "@types/node": "^25.9.5",
    "typescript": "^5.9.3"
  },
  "license": "MIT"
}
